@model IEnumerable<OnlyFarms.Models.Crop>

@{
    ViewData["Title"] = "Index";
}

<div class="text-center">
    <h1>Plony</h1>
</div>

@foreach (var item in Model)
{
    <div class="tile1">
        <div class="overlay1">
            <i class="icon-apple" style=" font-size: 70px;" onclick="location.href='Crops/Details/@item.ID';"></i>
            <a class="link">
                <i class="icon-trash" style="position:absolute; left:5px; top:0; color:aliceblue;" onclick="location.href='Crops/Delete/@item.ID';"></i>
                <p style="color:aliceblue;">@Html.DisplayFor(modelItem => item.CropName)</p>
                <i class="icon-pencil" style="position:absolute; right:5px; top:0; color:aliceblue;" onclick="location.href='Crops/Edit/@item.ID';"></i>
            </a>
        </div>
    </div>
}
<div class="tile1" onclick="location.href='Crops/Create';">
    <div class="overlay1">
        <i class="icon-plus" style=" font-size: 70px;"></i>
    </div>
</div>
